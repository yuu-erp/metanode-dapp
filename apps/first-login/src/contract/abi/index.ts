import { getActiveCodeInfo } from './get-active-code-info.abi'
import { getActiveCodesByDeviceInfo } from './get-active-codes-by-device-info.abi'

export const activeCodeAbi = {
  getActiveCodeInfo,
  getActiveCodesByDeviceInfo
}
